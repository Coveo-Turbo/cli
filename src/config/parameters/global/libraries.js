const common = [
    'coveo-search-ui',
];

const vanilla = [

];

const typescript = [
    '@coveops/turbo-core',
];

export default {
    "libraries.vanilla": [...common, ...vanilla],
    "libraries.typescript": [...common, ...typescript],
}